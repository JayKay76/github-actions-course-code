name: First Workflow
on: [push]

jobs:
    run-shell-coomands:
        runs-on: ubuntu-latest
        steps:
            - name: echo a string
              run: echo "Hello World"
            - name: MultiLine Command
              run: |
                node -v
                npm -v